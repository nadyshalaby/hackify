# Code Rules — Always-On (relocated)

**Canonical location moved.** As of v0.2.2 the content of this file lives at the plugin-root `rules/code-quality.md`. The new location is mirrored to every runtime by `scripts/sync-runtimes.sh` so consumers always find the canonical content — either at this path (legacy reference for in-skill cross-references) or at `rules/code-quality.md` (plugin-primitive location).

This stub stays in place to preserve existing intra-skill links. Read `rules/code-quality.md` for the canonical doctrine.
